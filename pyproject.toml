[project]
name = "llm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pyyaml>=6.0",
    "schedulefree>=1.4.1",
    "torch>=2.12.0",
    "transformer-engine>=2.15.0",
]
